enum RouterEnum {
  HOME = 'home',
  LOGIN = 'login',
  REGISTER = 'register',
  USERS = 'users',
  USER_FORM = 'userForm',
  PROFILE = 'profile',
  NOT_FOUND = 'notFound',
  BRAND = 'brand',
  CATEGORY = 'category',
}

export { RouterEnum }
